using System;

namespace PDC.Entity
{
    /// <summary>
    /// Enum to represent the Departments
    /// </summary>
    public enum Departments:Int16
    {
        Mail, Regular, Heavy, Insurance
    }
}
